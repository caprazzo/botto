# configure the botService
botService:
    # xmpp server host
    host: localhost

    # xmpp server Component port
    componentPort: 5275

    # xmpp server Component password
    componentSecret: secret

    # xmpp server standard client port
    clientPort: 5223

logging:
    level: INFO